import 'package:flutter/material.dart';

class CamPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This is cam page"),
      ),
    );
  }
}